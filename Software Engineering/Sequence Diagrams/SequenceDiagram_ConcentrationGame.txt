@startuml
Actor Human
human->pitboss:I_would_like_to_play_a_game
pitbos->artist:Querry_human(name)
artist->human:What_is_your_name?
human->pitboss:My_name_is_Don
pitboss->clerk:Give_me_info_on_(Don)
clerk-->pitboss:info
pitboss->artist:Display_and_querry(info,_number_of_pairs)
human-->pitboss:6
pitboss->dealer:prepare_game(6)
dearler->pitboss:game
pitboss->artist:Display(game)
human->pitboss:begin
pitboss->timerkeeper:begin_timing
human->pitboss:card(1,3)
pitboss->dealer:selected(card(1,3))
pitboss->artist:flip_card(card(1,3))
human->pitboss:card(1,4)
pitboss->artist:flip_card(card(1,4))
pitboss->dealer:selected(card(1,4))
dealer-->pitboss:no_match
dealer->artist:display(no_match)
human->pitboss:continue
pitboss->artist:flip_card(card(1,3))
pitboss->artist:flip_card(card(1,4))
human->pitboss:card(1,2)
pitboss->dealer:selected(card(1,2))
pitboss->artist:flip_card(card(1,2))
human->pitboss:card(1,3)
pitboss->artist:flip_card(card(1,3))
pitboss->dealer:selected(card(1,3))
dealer-->pitboss:match
dealer->artiest:display(match)
human->pitboss:continue
pitboss->artist:remove_card(card(1,2))
pitboss->artist:remove_card(card(1,3))
pitboss->timerkeeper:stop_timing
timerkeeper-->pitboss:6_minutes,_45_seconds
pitboss->clerk:store(6_minutes,_45_seconds)
pitboss->artist:Display(Congratulations_and_timeing)
artist->human:"Congratulations_you_took_6_minutes,_45_seconds"
@enduml

